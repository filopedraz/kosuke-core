# Python Preview image
This is the preview image of Python service whose package manager is `uv`.

## Repository requirements
The directory mounted as volume to this image should have the following prerequisites:
- `pyproject.toml` at root level containing the dependencies list.